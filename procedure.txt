When deploying heroku

1. Create Procfile and insert  => web: gunicorn app:app
which is the first file to exceute
app:app (filename : flaskappname (variable inside flask app.py))

2. Create requirements.txt
